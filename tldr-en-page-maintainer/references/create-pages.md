# Create Missing English Pages

Use these instructions only for a `create` operation.

- Require the exact target path to be absent from the fetched official upstream ref.
- Establish command identity, project eligibility, page type, platform, filename, heading, documentation URL, and required page relationships from current evidence.
- Build an evidence record for every proposed example and behavior claim before drafting.
- Create a complete page that follows current repository structure and deliberately prioritizes the most useful examples.
- Include any required base, alias-target, grouping, platform-variant, or disambiguation dependency as an explicit page in the batch; do not create it implicitly.
- For an alias page, use the repository's current generator and verify the generated form and target.

A `create` operation is complete when the new page is fully evidenced, structurally valid, within repository limits, and all uncertainties are recorded for human review.
